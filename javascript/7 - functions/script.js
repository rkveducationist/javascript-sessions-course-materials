        function sayHello() {
            document.write("Hi, hello, We are at Functions!");
        }

        function doCalc() {
            let res = a + b; 
            document.write('res: ' + res);
        }

        function makeMyCalc(num1, num2) {
            let res = num1 * num2; 
            return res;
        }